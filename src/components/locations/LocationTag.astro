---
export interface Props {
    tagColor: string;
}
const { tagColor } = Astro.props;

const tagBg = "bg-[" + tagColor.toString() + "]";
console.log(tagColor);
---

<link
    rel="stylesheet"
    href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
/>

<style>
    .material-symbols-outlined {
        font-variation-settings: "FILL" 0, "wght" 400, "GRAD" 0, "opsz" 48;
        display: flex !important;
        align-items: center;
        justify-content: center;
    }
</style>

<div
    class="border-solid rounded-md border-2 border-black-500 w-fit h-8 flex divide-solid divide-x overflow-hidden"
>
    <div class="flex justify-center w-8 h-8 bg-sky-400">
        <span class="material-symbols-outlined" style="color: white;">ac_unit</span>
    </div>
    <div class:list={["w-2 border-y-none"]} style=`background-color: ${tagColor};`></div>
    <div
        class="bg-red-500 w-auto border-y-none text-black flex items-center px-2"
        style=`background-color: ${tagColor}88;`
    >
        <p class="m-0">
            <slot />
        </p>
    </div>
</div>
<br />
<div
    class="border-solid rounded-md border-2 border-black-500 w-fit h-8 flex divide-solid divide-x overflow-hidden"
>
    <div class="flex justify-center w-8 h-8 bg-indigo-800">
        <span class="material-symbols-outlined" style="color: white;">severe_cold</span>
    </div>
    <div class:list={["w-2 border-y-none"]} style=`background-color: ${tagColor};`></div>
    <div
        class="bg-red-500 w-auto border-y-none text-black flex items-center px-2"
        style=`background-color: ${tagColor}88;`
    >
        <p class="m-0">Deep Freezer</p>
    </div>
</div>
